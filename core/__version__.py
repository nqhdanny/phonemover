"""PhoneMover version — single source of truth for the release tag.

Update this on every release, before pushing the version tag. The value
is consumed by:
  - GUI: shown in the header next to the logo (PhoneMover vX.Y.Z)
  - Packager: baked into the Windows exe metadata
"""

__version__ = "1.2.3"